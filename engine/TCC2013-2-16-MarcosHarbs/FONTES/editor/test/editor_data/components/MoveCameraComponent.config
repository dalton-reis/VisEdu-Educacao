new MoveCameraComponent().initialize(leftKey, 
           rightKey, 
           upKey, 
           downKey, 
           zoomInKey, 
           zoomOutKey,
           rotateKey,
           rotateInverseKey, 
           translateFactor, 
           scaleFactor,
           rotateFactor);